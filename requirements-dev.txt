# Dépendances de développement (tests, qualité de code).
# Installation : pip install -r requirements-dev.txt
-r requirements.txt
pytest>=7.4
ruff==0.15.19  # version épinglée pour un résultat identique local/CI/pre-commit
pre-commit>=3.5
# stable-baselines3 et torch sont désormais des dépendances runtime
# (voir requirements.txt) : inutile de les répéter ici.
